// AlgorithmComparison is superseded by GADecisionPanel.
// Kept as a file-level stub to avoid breaking imports.
import { OptimizationResponse } from '../types';

interface AlgorithmComparisonProps {
  result: OptimizationResponse | null;
}

export default function AlgorithmComparison({ result: _result }: AlgorithmComparisonProps) {
  return null;
}
